# portfolio

A new Flutter project.
### 2. Run generate code via build_runner

```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

### 3. Translate

input localization in l10n/intl_en.arb, ...

```bash
flutter pub run intl_utils:generate
```
